services:
  - type: web
    name: disaster-relief-app
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: honcho start
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.13.5"
    healthCheckPath: /
